# La cobertura minima (80%) vive en vitest.config.mts. Corre en cada commit
# para que el gate no dependa de acordarse, y lo mismo corre en CI para los
# aportes que llegan sin el hook instalado.
pnpm run test:coverage
