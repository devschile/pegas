# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Test coverage reports
coverage

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Respaldos locales de operaciones sobre git/n8n (nunca se versionan)
.rollback/

# Material de referencia local: piezas de diseño de terceros, notas de trabajo,
# transcripciones. Nada de esto se publica. Se ignora la carpeta entera y no
# archivo por archivo, porque el riesgo real es lo que se agregue sin pensar:
# context.md estuvo siete días en el repositorio público con la cadena de
# conexión a producción adentro.
/design/

# Un proyecto Nuxt no tiene HTML suelto en la raíz.
/*.html
design
context.md
